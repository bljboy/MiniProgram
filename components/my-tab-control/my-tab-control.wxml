<view class="tab-control">
  <block wx:for="{{titles}}" wx:key="index">
    <view data-index="{{index}}" 
    class="tab-item {{currentIndex == index ? 'active' : ''}}" bind:tap="handleItemClick">
    <text>{{item}}</text></view>
  </block>
</view>